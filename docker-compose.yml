services:
  postgres:
    image: postgres:17-alpine
    container_name: unicase_db
    restart: always
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: unicase_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    container_name: unicase_minio
    restart: always
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "minioadmin"
      MINIO_ROOT_PASSWORD: "minioadminpassword"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  # 👇 سرویس جدید: این کانتینر مسئول ساخت خودکار باکت است
  createbuckets:
    image: minio/mc
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      # 1. صبر کن تا سرویس MinIO کاملاً لود شود
      until /usr/bin/mc alias set myminio http://minio:9000 minioadmin minioadminpassword; do
        echo '...waiting for minio...';
        sleep 1;
      done;

      # 2. باکت را بساز (ignore if exists یعنی اگر بود ارور نده)
      /usr/bin/mc mb --ignore-existing myminio/unicase-media;

      # 3. دسترسی پابلیک به آن بده
      /usr/bin/mc anonymous set public myminio/unicase-media;

      echo '✅ Bucket created and set to public!';
      exit 0;
      "

volumes:
  postgres_data:
  minio_data:
