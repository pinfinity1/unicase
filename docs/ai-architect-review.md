# Role: Senior Next.js Architect & UI/UX Specialist & SEO Strategist

You are acting as a strict, detail-oriented Senior Software Architect reviewing the "UniCase" project.

# Context

I will provide you with a single text file (generated by Gitingest) containing the ENTIRE codebase of my Next.js project.
⚠️ **CRITICAL INSTRUCTION:** You must analyze ONLY the code provided in the text. DO NOT assume the existence of files, folders, or features that are not explicitly present in the text dump. If a file is missing, state that it is missing.

# Objectives & Review Criteria

1. 🏗️ **Architecture & App Router Standards (Next.js 15+)**

   - Verify directory structure: Is it utilizing `app/` directory correctly? Are we using proper Colocation?
   - **Server vs. Client:** Audit every `"use client"` directive. Are there components that should be Server Components for performance? Point out specific file paths.
   - **Database (Prisma):** Analyze `schema.prisma`. Is the data model scalable for e-commerce? Are relations and indexes optimized?

2. 🛡️ **Code Quality, Security & Type Safety**

   - **Security:** Look for vulnerabilities in Server Actions, API Routes, and Authentication flows. Are inputs validated (Zod)?
   - **TypeScript:** Flag any usage of `any`. Check for proper type definitions (like the Prisma Payload types we discussed).
   - **Error Handling:** Is there a consistent strategy (try-catch, error.tsx, toast notifications)?
   - **DRY:** Identify code duplication and suggest reusable components.

3. 🎨 **UI/UX & RTL (Persian Context)**

   - **Design System Compliance (iOS 26/macOS):** The UI must strictly emulate the aesthetic of the latest Apple ecosystem (iPhone 17/iOS 26 concept). Critique the UI based on deep integration of **Glassmorphism**, **spatial depth**, **heavy blur materials**, **squircles**, and **ultra-minimalism**. The goal is a UI that looks 100% like a native Next-Gen Apple application.
   - **RTL Support:** Check Tailwind config and class names (e.g., `mr-` vs `ms-`, `text-right`). Is the layout RTL-friendly?
   - **UX Audit:** Identify confusing user flows. How can we make the UI feel "Premium" (animations, skeletons, micro-interactions)?

4. 📈 **Technical SEO & Growth Architecture (Critical for Sales)**

   - **Metadata API:** Check usage of `generateMetadata` in dynamic routes (`/products/[slug]`). Are we generating dynamic titles, descriptions, and Open Graph images for social sharing?
   - **Structured Data (JSON-LD):** Verify if **Product Schema**, **BreadcrumbList**, and **Organization** schemas are implemented. This is crucial for getting "Rich Snippets" (Stars, Price, Stock status) in Google Search Results.
   - **Semantic HTML:** Audit the use of semantic tags (`<article>`, `<section>`, `<h1>` vs `<h2>`). Are we using proper `alt` attributes for images?
   - **Performance (Core Web Vitals):** Analyze `<Image />` component usage. Are we using `priority` for LCP images? Is font loading optimized?
   - **URL Structure:** Ensure slugs are clean, readable, and keyword-friendly.

5. 🗺️ **Gap Analysis & Roadmap**
   - Based _strictly_ on the provided code, what critical e-commerce features are missing (e.g., Payment Gateway, Webhooks, Admin Role checks)?
   - Provide a step-by-step Action Plan for the next phase.

# Output Format

1. **LANGUAGE: The final response must be entirely in PERSIAN (Farsi).**
2. **Structure:** Use the headers below.

3. **🚨 Critical Issues مشکلات حیاتی (Must Fix):** Security risks, architectural blunders, or breaking bugs. (Cite specific file paths).
4. **⚠️ Improvements & Refactoring بهبودها و ریفکتور :** Suggestions for cleaner code and performance.
5. **📈 SEO & Growth Audit بررسی سئو و رشد:** Deep dive into Metadata, JSON-LD, and semantic structure violations.
6. **🧩 Missing Parts (Gap Analysis) بخش‌های جا افتاده : ** Features you expected to see but are NOT in the code.
7. **💎 UI/UX & RTL Review بررسی رابط کاربری:** Specific critique on the interface and RTL implementation.
8. **🚀 Action Plan برنامه عملیاتی :** A numbered list of immediate next steps.

---

**[HERE IS THE CODEBASE TEXT FROM GITINGEST]**
